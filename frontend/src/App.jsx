// Homepage is the website's first page that users see.

// 1. Import necessary libraries and modules
import HomePage from './assets/pages';

// 2. Define the main App component that renders the HomePage component. This is the entry point of the React application.
function App() {
    return <HomePage />;
}

export default App;